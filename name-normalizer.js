(()=>{'use strict';const titleCaseName=s=>String(s??'').trim().toLocaleLowerCase().replace(/(^|[\s'-])([a-zà-ÿ])/g,(_,p,c)=>p+c.toLocaleUpperCase());document.addEventListener('click',event=>{const target=event.target instanceof Element?event.target:null;if(!target||target.id!=='usePaste')return;const input=document.getElementById('sheetPaste');const names=document.getElementById('names');if(!input||!names)return;const normalized=input.value.split(/\r?\n/).map(titleCaseName).filter(Boolean).join('\n');input.value=normalized;names.value=normalized},true)})();